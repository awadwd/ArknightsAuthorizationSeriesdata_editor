# DEPRECATED — Superseded by sync-knowcloud.yml (方案D, direct fetch).
# This file is archived here only for historical reference and MUST NOT be triggered on schedule.
# To re-enable (NOT recommended), restore schedule block below.
#
# on:
#   schedule:
#     - cron: '0 * * * *'
#   workflow_dispatch:

on:
  workflow_dispatch: # only manual; no cron = no hourly emails

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - name: This workflow is disabled
        run: |
          echo "Sync KnowCloud Data (legacy) was deprecated on 2026-09-04."
          echo "Active sync: .github/workflows/sync-knowcloud.yml (方案D, direct KnowCloud fetch)."
          echo "See memory/2026-09-04.md for migration history."